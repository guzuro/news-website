---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

export interface Props {
  title: string;
  description?: string;
  meta?: string;
  ogImage?: string;
}

const { title, description, meta, ogImage } = Astro.props;
---

<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="flex h-full flex-col text-white">
    <Header />
    <main class="flex-1">
      <div class="h-full bg-gray-800 py-16">
        <slot />
      </div>
    </main>
    <Footer />
  </body>
</html>
<style is:global></style>
